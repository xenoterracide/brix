commands:
  - exec:
      commands:
        [
          "cargo test --all",
          'echo "Hello World!"',
          'echo "MODULE: {{module}}, PROJECT: {{project}}"',
          "cargo --version",
        ]
      stdout: true
